import { FiltroArticuloPipe } from './filtro-articulo.pipe';

describe('FiltroArticuloPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArticuloPipe();
    expect(pipe).toBeTruthy();
  });
});
